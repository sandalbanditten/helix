//! The file tree docked beside the editor.

// The model is wired into `EditorView` by the next commit.
#![allow(dead_code)]

mod git;
mod icons;
mod ls_colors;
mod order;
mod rows;
mod tree;
mod viewport;
